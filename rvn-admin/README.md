# RVN Admin HTML

A framework-free admin dashboard HTML template inspired by the visual tone of RVN HTML and built with Vite, Tailwind CSS, vanilla JavaScript, Lucide icons, and ApexCharts.

## Quick Start

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
npm run preview
```

The compiled template is generated in `dist/`.
